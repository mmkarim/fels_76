<% provide :title, @title %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1><%= @user.name %></h1>
      <span><%= link_to t("view"), @user %></span>
    </section>
    <section class="stats">
      <%= render "shared/stats" %>
        <%= link_to @users %>
    </section>
  </aside>

</div>
