<% provide :title, t("users_all") %>

<h1><%= t "users_all" %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user %>
      <%= link_to user.name, user %>

    </li>
  <% end %>
</ul>

<%= will_paginate %>
